using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fifthBGM : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        musicctorl.instance.PlayBGM(2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
