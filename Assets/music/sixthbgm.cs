using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sixthbgm : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        musicctorl.instance.PlayBGM(6);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
